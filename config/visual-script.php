<?php

return [

    // پیشوند مسیرهای پنل مدیریت اسکریپت‌ها
    'route_prefix' => 'visual-script',

    'middleware' => ['web'],
    'api_middleware' => ['api'],

    // لیست سفید مدل‌هایی که در نود «فراخوانی از دیتابیس» قابل استفاده هستند.
    // کلید = نامی که کاربر در ویرایشگر ویژوال می‌بیند، مقدار = کلاس مدل واقعی.
    'models' => [
        // 'Post' => \App\Models\Post::class,
        // 'User' => \App\Models\User::class,
    ],

    // فیلدهای مجاز برای شرط‌گذاری روی هر مدل. اگر برای یک مدل خالی بماند،
    // همه‌ی ستون‌ها مجاز در نظر گرفته می‌شوند (برای پروژه‌های حساس حتما پر شود).
    'allowed_fields' => [
        // 'Post' => ['id', 'title', 'status', 'created_at'],
    ],

    // عملگرهای مجاز در شرط‌های نود کوئری
    'allowed_operators' => ['=', '!=', '>', '>=', '<', '<=', 'like', 'in', 'not in', 'null', 'not null'],

    // حداکثر تعداد رکورد قابل بازگشت توسط یک نود کوئری
    'max_query_limit' => 500,

    // حداکثر تعداد تکرار مجاز در نود حلقه
    'max_loop_iterations' => 1000,

    // حداکثر عمق تودرتو بودن نودها (شرط/حلقه داخل شرط/حلقه)
    'max_nesting_depth' => 15,
];
